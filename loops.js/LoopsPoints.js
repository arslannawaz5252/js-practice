// for...in → keys only (strings).

// for...of → works on iterables (like arrays, Maps, or Object.entries).

// That’s why you never write [key, value] in object — because for...in doesn’t give you pairs.


----//for of loop points
// That [key, value] syntax is array destructuring — it takes an array and
//  unpacks its elements into separate variables___