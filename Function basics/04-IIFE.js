


(function chai () {
    // name iife
    console.log(`Db connected`);
    
    
})();  // to connect two iife (;) is is used at the end of the one

// that iife function is like { chai() } so there is no other myth

( ()=>{
    console.log(`Db connectd to`);
    
})()

 // if they said to you to put parametre and execute so

 ( (name)=>{
    console.log(`Db connectd to,${name}`);
    
})('Arslan')

// we used it to avoid global scope pollution of global variable 
