// javasricpt and classes

//OOP

// object
--collection OF properties(variables) and methods(function)
--toLowerCase


// Why use oop 

//Encapsulation (Data Protection)

    //OOP allows hiding implementation details inside classes (using private fields #property in modern JS).
    //OOP in JavaScript helps you model real-world entities, write cleaner code, avoid duplication, and make future changes easier — essential for large apps, games, frameworks, or APIs.
    // Polymorphism (Same Method, Different Behavior)

    // object literals

    -- constructor;
    --classs;
    --function;
    --prototype;
    --instances(this,new);